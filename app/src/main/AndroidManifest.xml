<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FootballHub">

        <!-- Màn hình đăng nhập là điểm khởi chạy duy nhất -->
        <activity
            android:name=".Activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Activities.RegisterActivity" android:exported="false" />

        <activity android:name=".Activities.MainActivity" android:exported="false" />

        <!-- Day 2: Team Management (Full CRUD) -->
        <activity android:name=".Activities.TeamManagementActivity" android:exported="false" />
        <activity android:name=".Activities.AddTeamActivity"        android:exported="false" />
        <activity android:name=".Activities.TeamDetailActivity"     android:exported="false" />

        <!-- Day 3: Player Management (Full CRUD) -->
        <activity android:name=".Activities.PlayerManagementActivity" android:exported="false" />
        <activity android:name=".Activities.AddPlayerActivity"        android:exported="false" />
        <activity android:name=".Activities.PlayerDetailActivity"     android:exported="false" />

        <!-- Day 4: Tournament Management (Full CRUD) -->
        <activity android:name=".Activities.TournamentManagementActivity" android:exported="false" />
        <activity android:name=".Activities.AddTournamentActivity"        android:exported="false" />
        <activity android:name=".Activities.TournamentDetailActivity"     android:exported="false" />
        <activity android:name=".Activities.DrawCeremonyActivity"         android:exported="false" />

        <!-- Match Schedule & Results -->
        <activity android:name=".Activities.MatchScheduleActivity" android:exported="false" />
        <activity android:name=".Activities.AddMatchActivity"      android:exported="false" />
        <activity android:name=".Activities.MatchDetailActivity"   android:exported="false" />

        <!-- Statistics -->
        <activity android:name=".Activities.StatisticsActivity"    android:exported="false" />

    </application>

</manifest>